import React from "react";
import LoginComponent from "./Component/LoginPresenter";
const LoginContainer = (props) => {
  return <LoginComponent {...props}></LoginComponent>;
};
export default LoginContainer;
